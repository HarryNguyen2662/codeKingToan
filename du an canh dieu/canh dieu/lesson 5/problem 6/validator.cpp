#include "testlib.h"
#include <bits/stdc++.h>

using namespace std;

const int INF = 100;

int main(int argc, char *argv[])
{
    registerValidation(argc, argv);
    inf.readString();
    int n = inf.readInt(-INF, INF, "out of range");
    inf.readEoln();
    while(n--)
    {
        inf.readString();
    }
    inf.readEof();
    return 0;
}
