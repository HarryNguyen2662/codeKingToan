#include "testlib.h"
#include <bits/stdc++.h>

using namespace std;

const int INF = 1000;

int main(int argc, char *argv[])
{
    registerValidation(argc, argv);
    inf.readInt(1, INF, "out of range");
    inf.readSpace();
    inf.readInt(1, INF, "out of range");
    inf.readSpace();
    inf.readInt(1, INF, "out of range");
    inf.readEoln();
    inf.readEof();
    return 0;
}
