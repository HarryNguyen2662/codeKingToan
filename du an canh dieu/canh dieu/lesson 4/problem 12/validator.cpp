#include "testlib.h"
#include <bits/stdc++.h>

using namespace std;

const double INF = 1000;

int main(int argc, char *argv[])
{
    registerValidation(argc, argv);
    inf.readReal(1, INF, "out of range");
    inf.readSpace();
    inf.readReal(1, INF, "out of range");
    inf.readEoln();
    inf.readEof();
    return 0;
}
